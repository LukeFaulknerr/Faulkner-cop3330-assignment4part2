package ucf.assignments;

import java.util.ArrayList;

public class fileSaveLoad {
    public boolean saveList(TDList list){
        // Saves list to storage
        // Returns true if successful
        return true;
    }

    public boolean saveLists(ArrayList<TDList> lists){
        // Save lists to storage
        // Returns true if successful
        return true;
    }

    public boolean loadList(TDList list){
        // Loads list from storage
        // Returns true if successful
        return true;
    }

    public boolean loadLists(ArrayList<TDList> lists){
        // Load lists from storage
        // Returns true if successful
        return true;
    }
}
