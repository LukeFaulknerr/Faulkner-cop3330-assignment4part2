Main
    Launch window
Start
    Create root BorderPane and initial Scene
    Create MenuBar

    Create a file menu for importing/exporting
    Add all necessary options to the menu

    Create a ToDoList menu with options for making a new list, editing, deleting, displaying
    Add all necessary options to the menu

    Create an editToDoList menu with options for altering the items in a todoList
    Add all necessary items to the list

    If file menu is pressed
        Display options
        Send button choice to external class to do the work
    If ToDoList is pressed
        Display the lists
        Display actions
        Send button choice to external class to do the work
    If editToDoList is pressed
            Display the lists
            Display actions
            Allow for user to select action and list
            Send those choices to an external class


