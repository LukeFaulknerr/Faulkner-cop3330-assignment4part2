package ucf.assignments;

public class TDListDisplay {
    public boolean displayIncomplete(TDList list){
        // Iterate through list, only display if TD item is incomplete
        // Return true if process was successful, false if not
        return true;
    }

    public boolean displayComplete(TDList list){
        // Iterate through list, only display if TD item is complete
        // Return true if process was successful, false if not
        return true;
    }
}
